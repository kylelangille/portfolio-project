import { styled } from "styled-components";
import Wrapper from "./UI/Wrapper";

const Projects = () => {
  return (
    <Wrapper>
      <section id="projects">
        <h2>Projects</h2>
      </section>
    </Wrapper>
  );
};

export default Projects;
